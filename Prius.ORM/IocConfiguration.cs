namespace Prius.Orm
{
    /*
    public class IocConfiguration : IIocConfig
    {
        public int OrderIndex { get { return 200; } }

        public void RegisterDependencies(IPropertyBag iocConfig, IIocRegistrar registrar)
        {
            registrar.RegisterSingleton<ICommandFactory, CommandFactory>(f => f.Initialize());
            registrar.RegisterSingleton<IConnectionFactory, ConnectionFactory>(f => f.Initialize());
            registrar.RegisterSingleton<IContextFactory, ContextFactory>(f => f.Initialize());
            registrar.RegisterSingleton<IDataEnumeratorFactory, DataEnumeratorFactory>(f => f.Initialize());
            registrar.RegisterSingleton<IDataReaderFactory, DataReaderFactory>(f => f.Initialize());
            registrar.RegisterSingleton<IMapper, Mapper>(i => i.Initialize());
            registrar.RegisterSingleton<IParameterFactory, ParameterFactory>(f => f.Initialize());
            registrar.RegisterSingleton<IRepositoryFactory, RepositoryFactory>(rpf => rpf.Initialize());
            registrar.RegisterSingleton<IEnumerableDataFactory, EnumerableDataFactory>(rpf => rpf.Initialize());
        }

        public void RegisterInstances(IPropertyBag iocConfig, IIocRegistrar registrar)
        {
        }
    }
    */
}
